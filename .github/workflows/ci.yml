name: Cypress and Jest Tests

on:
  push:
    branches:
      - 'acceptance'
      - 'main'

jobs:
  tests:
    runs-on: ubuntu-latest
    env:
      REACT_APP_GOOGLE_MAPS_API_KEY: ${{ secrets.REACT_APP_GOOGLE_MAPS_API_KEY }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Install and Cache NPM dependencies
      - name: Install dependencies
        run: |
          cd web/src
          npm ci --verbose

      # # Run Jest tests with coverage
      # - name: Run Jest tests
      #   run: |
      #     cd web/src
      #     npm test -- --coverage
      # Run Jest tests with coverage
      - name: Run Jest tests
        run: |
          cd web/src
            npm test


      # Run Cypress tests
      - name: Cypress run
        uses: cypress-io/github-action@v6
        with:
          working-directory: web/src/tests
          start: npx vite --host

      # Upload Jest coverage report as an artifact (so SonarQube can use it)
      - name: Save Jest Coverage Report
        uses: actions/upload-artifact@v4
        with:
          name: jest-coverage
          path: web/src/coverage/lcov.info

  sonarqube:
    name: SonarQube
    runs-on: ubuntu-latest
    needs: tests  # Wait for tests to finish before running SonarQube
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Download the Jest coverage report
      - name: Download Jest Coverage Report
        uses: actions/download-artifact@v4
        with:
          name: jest-coverage
          path: web/src/coverage

      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
