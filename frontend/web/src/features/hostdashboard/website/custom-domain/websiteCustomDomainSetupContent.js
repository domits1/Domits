export const WEBSITE_CUSTOM_DOMAIN_SETUP_STEPS = [
  "Choose a subdomain you control, such as stay.example.com.",
  "Save that hostname here so Domits can reserve it for this published website.",
  "Keep the Domits live link online while the custom domain stays pending.",
  "After routing and certificate readiness are confirmed, the custom domain can be activated.",
];

export const WEBSITE_CUSTOM_DOMAIN_DEFAULT_RECOMMENDATION =
  "Use a subdomain you control, such as stay.example.com.";

export const WEBSITE_CUSTOM_DOMAIN_DEFAULT_NEXT_ACTION =
  "Domits still needs to verify routing and certificate readiness before the custom domain can replace the fallback live link.";
